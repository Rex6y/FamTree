<Page x:Class="WpfApp1.PageCreate"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageCreate">

    <Grid Background="#FAFAFA">
        <Button x:Name="BackButton"
                    Content="←"
                    FontSize="22"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="10,0,0,0"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="BackButton_Click"
                    Cursor="Hand"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" x:Name="FormContainer" MaxWidth="400"
                HorizontalAlignment="Stretch" VerticalAlignment="Center">
                
                <Border Width="150" Height="150"
                CornerRadius="0"
                BorderBrush="#555"
                BorderThickness="1"
                Margin="0,20,0,0">
                    <Image x:Name="Pfp" Stretch="UniformToFill" MouseLeftButtonUp="Pfp_click" Cursor="Hand" Source="C:\Users\Admin\Pictures\Firefly appreciation\ff.jpg"/>
                </Border>
                
                <Grid Margin="20,20,20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Họ và Tên:" VerticalAlignment="Center" Margin="0,0,5,10"/>
                    <TextBox Grid.Row="0" x:Name="FullName" Grid.Column="1" Height="30" Margin="0,0,0,10" VerticalContentAlignment="Center"/>

                    <TextBlock Grid.Row="1" Text="Giới tính:" VerticalAlignment="Center" Margin="0,0,5,10"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                        <RadioButton Content="Nam" x:Name="GenderMale" Margin="0,0,20,0"/>
                        <RadioButton Content="Nữ" x:Name="GenderFemale"/>
                    </StackPanel>

                    <TextBlock Grid.Column="0" Grid.Row="2" Text="Ngày sinh:" VerticalAlignment="Center" Margin="0,0,5,10"/>
                    <DatePicker Grid.Column="1" Grid.Row="2" x:Name="Birthdate" Height="30" 
                                SelectedDateFormat="Short" HorizontalAlignment="Stretch" Margin="0,0,0,10" VerticalContentAlignment="Center">

                    </DatePicker>
                </Grid>
                <Button Content="Lưu"
                Margin="0,20,0,0"
                Height="40" Width="100"
                HorizontalAlignment="Center"
                Click="CreatePerson_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
